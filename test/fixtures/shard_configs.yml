# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
# Note: quotes around value of 'on' are essential to get literal value of 'on' 
# loaded into the column rather than t.
# == Schema Information
#
# Table name: shard_config
#
#  id         :bigint           not null, primary key
#  deprecated :boolean          default(FALSE), not null
#  name       :string(255)      not null
#  use_notes  :string(255)
#  value      :string(5000)     not null
#
name-parent-rank-restriction: 
  name: name parent rank restriction 
  value: "on"
classification-tree-key:
  name: classification tree key
  value: APC
name-space:
  name: name space
  value: anamespace
