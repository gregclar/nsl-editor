<% if can? :update, @author %>
  <% increment_tab_index(0) %>

  <%= render 'form' %>
<% end %>

<%= divider %>
<% if can? :destroy, @author %>
  <% if @author.can_be_deleted? %>
    <%= render 'delete_widgets' %>
  <% else %>
    <%= render 'cannot_delete' %>
  <% end %>
<% end %>
