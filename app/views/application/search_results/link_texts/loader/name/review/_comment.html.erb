
<%= "#{search_result.comment}" %>
by <%= "#{search_result.batch_reviewer.user.given_name}" %> <%= "#{search_result.batch_reviewer.user.family_name}" %>
on <%= "#{search_result.created_at.strftime("%d-%b-%Y")}" %>

