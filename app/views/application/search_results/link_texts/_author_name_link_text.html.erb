<% if search_result.extra_information.present? %>
  <%= "#{search_result.name} (#{search_result.extra_information})" %>
<% else %>
  <%= "#{search_result.name}" %>
<% end %>
<% if search_result.duplicate? %> [Duplicate] <% end %>


