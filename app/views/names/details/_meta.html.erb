Name #<%= @name.id %>
<br>
<%= created_by_whom_and_when(@name).html_safe %>
<br>
<%= updated_by_whom_and_when(@name).html_safe %>
<% if @name.source_system %>
  <br>Migrated from <%= "#{@name.source_system.downcase} ##{@name.source_id}" %>
<% end %>

