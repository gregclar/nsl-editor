  Cannot delete:
  <% if @name.instances.size > 0 %>
    <br>
    <%= link_to("#{pluralize(@name.instances.size,'instance')} #{editor_icon('search')}".html_safe, 
                        search_path(query_string: "id: #{@name.id} show-instances:", 
                        query_target: 'name'),
                        tabindex: increment_tab_index,
                        title: "Search for the #{pluralize(@name.instances.size,'instance')}",
                        class: '') %>
  <% end %>
  <% if @name.children.size > 0 %>
    <br>
  <%= link_to("#{pluralize(@name.children.size,'child')} #{editor_icon('search')}".html_safe,
    search_path(query_string: "parent-id: #{@name.id}",
                query_target: 'name'),
                tabindex: increment_tab_index,
                title: "Retrieve #{pluralize(@name.children.size,'child')}.",
                class: '') %>
  <% end %>
  <% if @name.family_dependents? %>
    <br>
  <%= link_to("#{pluralize(@name.family_members&.size,'family member')} #{editor_icon('search')}".html_safe,
    search_path(query_string: "family-id: #{@name.id}",
                query_target: 'name'),
                tabindex: increment_tab_index,
                title: "Retrieve #{pluralize(@name.family_members&.size,'family member')}.",
                class: '') %>
  <% end %>

  <% if @name.comments.size > 0  %>
    <br>
    <%= pluralize(@name.comments.size,'Comment') %>
  <% end %>
  <% if @name.duplicates.size > 0  %>
    <br>
    <%= render partial: "names/widgets/duplicates" %>
  <% end %>


