<% increment_tab_index(0) %>
Diff
work-in-progress
<% unless params["tree-element-operation"].blank? %>
  operation: <%= params["tree-element-operation"] %>
  <br>
  tree version: <%= params["tree-version-id"] %>
  <br>
  tree element: <%= @tree_element.id %>
  <br>
  previous tree version: <%= TreeVersion.find(params["tree-version-id"]).previous_version_id %>
  <br>
  <%= params.inspect %>
  <br>
  <%# e1 is the older one %>
  <% e1 = "/tree/#{TreeVersion.find(params["tree-version-id"]).previous_version_id}/#{@tree_element.previous_element_id}" %>
  <%= e1 %>
  <br>
  <% e2 = "/tree/#{params['tree-version-id']}/#{@tree_element.id}" %>
  <%= e2 %>
  <br>
  <%= link_to 'Link link to local services', "#{Rails.configuration.services_clientside_root_url}tree-version/diff-element?e1=#{CGI.escape(e1)}&e2=#{CGI.escape(e2)}" %>
  <br>
  <%= link_to 'Link to test', "https://test.biodiversity.org.au/nsl/services/tree-version/diff-element?e1=#{CGI.escape(e1)}&e2=#{CGI.escape(e2)}" %>
<% end %>
