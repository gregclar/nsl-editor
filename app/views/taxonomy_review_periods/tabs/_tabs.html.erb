<ul class="nav nav-tabs">
  <%= render partial: 'taxonomy_review_periods/tabs/tab', locals: {first: true,
                                      last: true,
                                      this_tab: 'tab_details', 
                                      link_text: 'Details', 
                                      link_id: 'taxonomy-review-period-details-tab', 
                                      link_title: 'View a summary',
                                      tab_index: @tab_index,
                                      prev_tab: 'tab_delete', 
                                      next_tab: 'tab_edit' } %> 

  <% if can? 'taxonomy_review_periods', 'update' %>

  <%= render partial: 'taxonomy_review_periods/tabs/tab', locals: {first: false,
                                      last: false,
                                      this_tab: 'tab_edit', 
                                      link_text: 'Edit', 
                                      link_id: 'taxonomy-review-period-edit-tab', 
                                      link_title: 'Edit the taxonomy review period',
                                      tab_index: increment_tab_index,
                                      prev_tab: 'tab_details', 
                                      next_tab: 'tab_delete'} %> 


  <% end %>

  <% if can? 'taxonomy_review_periods', 'delete' %>

  <%= render partial: 'taxonomy_review_periods/tabs/tab', locals: {first: false,
                                      last: false,
                                      this_tab: 'tab_delete', 
                                      link_text: 'Delete', 
                                      link_id: 'taxonomy-review-edit-tab', 
                                      link_title: 'Delete the taxonomy review period',
                                      tab_index: increment_tab_index,
                                      prev_tab: 'tab_edit', 
                                      next_tab: 'tab_details'} %> 


  <% end %>

</ul>
