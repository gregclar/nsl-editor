

<% if session[:include_common_and_cultivar] == true %>
  <% icon_name = 'check-square-o fa-lg' %>
  <% title_text = 'Name searches will include common and cultivars' %>
<% else %>
  <% icon_name = 'square-o fa-lg' %>
  <% title_text = 'Name searches will exclude common and cultivars' %>
<% end %>

<%= link_to "#{icon(icon_name)} Common/Cultivars".html_safe,
  set_include_common_and_cultivar_path,
  remote: true,
  method: :post,
  title: "#{title_text}",
  class: "" %>

&nbsp;

