$('.message-container').html('');
$('.error-container').html('');
$('#search-result-details-info-message-container').html("<%= escape_javascript(@message) %>... Tab will be refreshed.");
setTimeout(
  function() 
  {
    $('#loader-name-ref-instance-tab').click();
  }, 1500
);
