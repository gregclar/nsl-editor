
<br>
    <%= form_with model: name_review_comment, url: 'name_review_comments', method: :post do |form| %>
      <%= form.hidden_field :batch_reviewer_id %>
      <%= form.hidden_field :loader_name_id %>
      <%= form.hidden_field :review_period_id %>
      Type*:<br>
      <%= form.collection_select :name_review_comment_type_id, 
        Loader::Name::Review::Comment::Type.for(tab_focus, @current_user.compiler?).order(:name),
        :id,
        :name,
        {},
        class: 'form-control',
        title: 'Type or topic of the comment' %> 

      Comment*: <%= form.text_area :comment, class: 'form-control give-me-focus', title: "Enter comment", required: true, tabindex: increment_tab_index %>
      <%= form.submit id: "save-new-batch-review-comment-btn", class: 'btn btn-primary', title: "Save comment", tabindex: increment_tab_index %>
      
      <br>
  <% end %>

