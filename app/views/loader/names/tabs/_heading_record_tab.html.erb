<ul class="nav nav-tabs">
Heading entry
</ul>

<% if @loader_name.rank == 'family' %>
  <%= render partial: 'detail_line', 
             locals: {info: link_to("Members of #{@loader_name.simple_name}",
         search_path(query_target: "loader names",
                     query_string: "family-id: #{@loader_name.id}",
                     focus_id: "loader-name-#{@loader_name.id}"),
                     title: 'Query family members',
                     class: 'blue underline'), 
                    label: 'query family members'} %>
<% end %>


