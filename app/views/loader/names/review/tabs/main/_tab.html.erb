<% this_tab_re = Regexp.new(this_tab) %>

<% data = Hash.new %>
<% data['edit-url'] = loader_name_review_tab_path(@loader_name,tab:"#{this_tab}", component: display_component || @loader_name.record_type) %>
<% data['tab-url']  =  loader_name_review_tab_path(id: @loader_name,tab:"#{this_tab}", component: display_component || @loader_name.record_type) %>
<% data['prev-tab'] = prev_tab unless prev_tab.blank? %>
<% data['tab-name'] = this_tab %>

<li class='first <%= @tab.match(this_tab_re) ? "active" : "non-active" %>'>
 <%= link_to link_text,
             '#',
             id: link_id,
             class: 'tab edit-details-tab', 
             title: link_title,
             tabindex: tab_index,
             data: data %>
</li>

