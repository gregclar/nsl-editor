<div id="batch-loader-control-panel" class="reports-inner">

<% if session[:default_loader_batch_id].blank? %>
  <h5>Please select a default batch</h5>
  <p>
  This tab works on the default batch for your Editor session, so please choose a batch.
  </p>
  <p>
  You can switch the default batch anytime.
  </p>
  <%= render partial: 'detail_line',
    locals: {label: 'query the batch stacks',
             info: link_to("List Batch Stacks",
                          search_path(
                            query_string: "*",
                            query_target: 'batch stacks',
             ),
                            title:"Query batch in stack format",class:'blue')} %>

<% else %>
  <h3>Default batch: <%= session[:default_loader_batch_name] %></h3>
  <% increment_tab_index(0) %>
  <h4>Loading Taxonomy Data</h4>
  <%= render partial: 'loader/batches/bulk/overview' %>
  <h4>Bulk Operations</h4>
  <%= render partial: 'loader/batches/bulk/form' %>
  <%= render partial: 'loader/batches/bulk/stats' %>
<% end %>

</div>
